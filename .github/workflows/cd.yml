name: 🚀 Continuous Deployment

on:
  workflow_run:
    workflows: ["🔄 CI Pipeline"]
    types: [completed]
    branches: [main]
  
  # Déploiement manuel
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment'
        required: true
        default: 'staging'
        type: choice
        options: [staging, production]

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/cesi-zen

jobs:
  # Build et Push des images Docker
  build-push:
    name: 🏗️ Build & Push Docker Images
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch'

    strategy:
      matrix:
        service: [backend, frontend]

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v4

      - name: 🐳 Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔑 Login to Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Get version
        id: version
        run: |
          if [[ "${{ github.ref }}" =~ refs/tags/v.* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
          else
            VERSION=$(git rev-parse --short HEAD)
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: 🏗️ Build and Push
        uses: docker/build-push-action@v5
        with:
          context: ./cesi-zen-code/${{ matrix.service == 'backend' && 'backend-ts' || 'frontend' }}
          file: ./cesi-zen-code/${{ matrix.service == 'backend' && 'backend-ts' || 'frontend' }}/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}-${{ matrix.service }}:${{ steps.version.outputs.version }}
            ${{ env.IMAGE_NAME }}-${{ matrix.service }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Déploiement
  deploy:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    needs: build-push

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v4

      - name: 🏷️ Get version
        id: version
        run: |
          if [[ "${{ github.ref }}" =~ refs/tags/v.* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
          else
            VERSION=$(git rev-parse --short HEAD)
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: 🚀 Deploy with Docker Compose
        run: |
          echo "🚀 Déploiement de la version ${{ steps.version.outputs.version }}"
          
          # Variables d'environnement pour le déploiement
          export DOCKER_TAG=${{ steps.version.outputs.version }}
          export IMAGE_BACKEND=${{ env.IMAGE_NAME }}-backend:${{ steps.version.outputs.version }}
          export IMAGE_FRONTEND=${{ env.IMAGE_NAME }}-frontend:${{ steps.version.outputs.version }}
          
          echo "Images à déployer :"
          echo "  Backend: $IMAGE_BACKEND"
          echo "  Frontend: $IMAGE_FRONTEND"
          
          # Ici, vous ajouteriez vos commandes de déploiement réelles :
          # 
          # Pour un serveur distant :
          # ssh user@server "cd /app && docker compose pull && docker compose up -d"
          #
          # Pour Railway :
          # railway deploy
          #
          # Pour Heroku :
          # heroku container:push web --app cesi-zen-api
          # heroku container:release web --app cesi-zen-api
          #
          # Pour DigitalOcean App Platform :
          # doctl apps create-deployment <app-id>

      - name: 🏥 Health Check
        run: |
          echo "🏥 Vérification de l'application..."
          
          # Attendre que les services démarrent
          sleep 30
          
          # Health checks (à adapter selon votre environnement)
          # curl -f https://your-domain.com/api/health || exit 1
          # curl -f https://your-domain.com/ || exit 1
          
          echo "✅ Application déployée et fonctionnelle"

      - name: 🎉 Success
        run: |
          echo "🎉 Déploiement réussi !"
          echo "Version: ${{ steps.version.outputs.version }}"
          echo "Images disponibles sur: ${{ env.REGISTRY }}/${{ github.repository_owner }}/cesi-zen"